<?php

// Establish database connection
$servername = "localhost";
$username = "root";
$password = "";
$dbname = "your_database_name";
$conn = new mysqli($servername, $username, $password, $dbname);

// Check connection
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

// Fetch list of providers from the database
$sql = "SELECT * FROM providers";
$result = $conn->query($sql);

if ($result->num_rows > 0) {
    // Create an array to store the providers
    $providers = array();

    // Loop through each row and add the provider to the array
    while ($row = $result->fetch_assoc()) {
        $providers[] = $row;
    }

    // Convert the array to JSON and output the result
    echo json_encode($providers);
} else {
    echo "No providers found";
}

$conn->close();
?>
===================
2o Αρχείο PHP
==========================
<?php
// Retrieve data from the Android app
$name = $_POST['name'];
$description = $_POST['description'];

// Establish database connection
$servername = "localhost";
$username = "root";
$password = "";
$dbname = "your_database_name";
$conn = new mysqli($servername, $username, $password, $dbname);

// Check connection
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

// Insert new provider into the database
$sql = "INSERT INTO providers (name, description) VALUES ('$name', '$description')";

if ($conn->query($sql) === TRUE) {
    echo "New provider added successfully";
} else {
    echo "Error: " . $sql . "<br>" . $conn->error;
}

$conn->close();
?>
